<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- For crash detection (sensors) -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <!-- For internet connectivity checks -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- For vibration feedback on crash detection -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- For waking up device on crash detection -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- For running foreground service (required for Android 9+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- For special use foreground service type (Android 14+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PothBondhu"
        tools:targetApi="31">

        <!-- OpenStreetMap User Agent (Required for OSM) -->
        <meta-data
            android:name="org.osmdroid.USER_AGENT"
            android:value="PothBondhu/1.0 (Android)" />

        <!-- Splash Screen Activity (Launcher) -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.PothBondhu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Signup Activity -->
        <activity
            android:name=".SignupActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Main Activity (Dashboard with Map) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Crash Detection Service (Foreground Service) -->
        <service
            android:name=".CrashDetectionService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Crash detection and emergency alerts" />
        </service>

    </application>

</manifest>